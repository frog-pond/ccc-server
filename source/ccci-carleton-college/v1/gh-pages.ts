export const GH_PAGES = (filename: string) => new URL(`https://carls-app.github.io/carls/${filename}`)

export const MAP_DATA = (filename: string) => new URL(`https://carls-app.github.io/map-data/${filename}`)
